{
  "name": "newman",
  "version": "1.0.0",
  "description": "project for storing postman tests",
  "directories": {
   "test": "tests"
  },
  "scripts": {
    "api-tests-production": "newman run tests/newman.postman_collection.json"
  },
  "author": "iryna",
  "dependencies": {
    "newman": "^6.1.3"
  }
}
